class Obstacle {
  //Obstacles include the two enemy types and rocks
  //all Obstacles havwe a move and show
  //move includes wandering and chasing the player
  constructor(type,x,y) {
    this.type = type;
    this.x = x;
    this.y = y;
  }
  
  move(){
    
  }
  
  show(){
    
  }
}
